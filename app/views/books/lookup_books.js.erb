$('#search_results').html('');
$('#search_results').removeClass('loading');

<%  @books.each do |item| %>
  $('#search_results').append('<%= strip_newlines render('amazon_book_result', :book => item) %>');
<% end %>
