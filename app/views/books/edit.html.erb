<%= render 'shared/book', :book => @book, :user => nil %>
<p>
<%= form_tag(book_path(@book.asin), :remote => :true, :method => :put) do %>
  <div class="clearfix">
    <div class="row">
      <div class="span5 columns">
        <%= label_tag 'start_date', 'Start Date' %>
        <div class="input">
          <%= text_field_tag 'start_date', format_date(@log.start_date), :class => 'medium date-picker' %>
        </div>
      </div>
      <div class="span5 columns">
        <%= label_tag 'finish_date', 'Finish Date' %>
        <div class="input">
          <%= text_field_tag 'finish_date', format_date(@log.finish_date), :class => 'medium date-picker' %>
        </div>
      </div>
      <div class="span5 columns">
        <div class="align-center">
          <%= submit_tag 'Update', :class => 'btn primary' %>
          <%= link_to(book_path(@book.asin), :remote => true, :method => :delete) do %>
            <span class="btn danger">Remove from list!</span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>
</p>
<p>
  <sub>Add the dates when you started your book, and when you finished it. If you only add the start date its fine, it means you are still reading it!</sub>
</p>
<script type="text/javascript">
$(document).ready(function () {
    $('.date-picker').datepicker();
});
</script>